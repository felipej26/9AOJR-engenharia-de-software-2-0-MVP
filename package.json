{
  "name": "financial-control-mvp",
  "version": "1.0.0",
  "private": true,
  "description": "MVP Controle Financeiro Pessoal",
  "scripts": {
    "dev": "cd backend && npm run dev",
    "setup": "cd backend && npm install && npm run setup",
    "start": "cd backend && npm run start"
  }
}
